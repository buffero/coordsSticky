<Window x:Class="ShowCoords.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShowCoords"
        mc:Ignorable="d"
        Title="MainWindow" Height="60" Width="230" WindowStyle="None">
    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="100"/>
            <ColumnDefinition Width="Auto" MinWidth="100"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Height="24" Text="{Binding CoordsX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="1"/>
        <TextBox Height="24" Text="{Binding CoordsZ, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="1" Grid.Column="1"/>
        <TextBox Grid.Row="1" Text="{Binding NetherCoordsX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="1" Height="24"/>
        <TextBox Grid.Row="1" Text="{Binding NetherCoordsZ, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="1" Height="24" Grid.Column="1"/>
    </Grid>
</Window>
